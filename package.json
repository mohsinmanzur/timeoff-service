{
  "name": "wizda-monorepo",
  "version": "1.0.0",
  "scripts": {
    "start:all": "concurrently \"npm run start --prefix hcm-service\" \"npm run start --prefix timeoff-service\"",
    "test:all": "npm test --prefix hcm-service && npm test --prefix timeoff-service"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
